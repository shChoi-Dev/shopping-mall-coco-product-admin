# 🛍️ Coco Shopping Mall (포트폴리오)
**"사용자 후기 중심 뷰티 커머스 플랫폼"**

## 🚀 배포 및 시연 (Deployment & Demo)

| 서비스 바로가기 및 계정 정보 | 프로젝트 시연 영상 |
| :--- | :--- |
| • **Frontend (Vercel)**: [바로가기](https://shopping-mall-coco-product-admin.vercel.app/) <br> • **Backend API (Render)**: [바로가기](https://coco-back.onrender.com) <br>🚨(무료 서버 특성상 초기 접속 시 3~5분 정도 소요) <br><br> **[테스트 계정]** <br> • **일반 유저**: `test01` / `test1234` <br> • **관리자**: `admin` / `admin1234` | <a href="https://www.youtube.com/watch?v=7Ta6qJzgdss" target="_blank"><img src="https://img.youtube.com/vi/7Ta6qJzgdss/0.jpg" width="320px" alt="시연영상"></a><br>*(이미지 클릭 시 이동)* |

> **프로젝트 개요**
> - **수행 기간**:
>   - Ver 1.0 (초기 팀 프로젝트): 2025.10 ~ 2025.12, 2개월
>   - Ver 2.0 (개인 고도화): 2026.03 ~ 2026.04, 1개월
> - **참여 인원**: 6명 (Ver 1.0 팀 프로젝트) → 1명 (Ver 2.0 개인 고도화)
> - **담당 역할**: 상품 및 관리자 도메인 설계, UI/UX 개선, 클라우드 인프라 마이그레이션 및 배포
> - **기술 스택**: Java 21, Spring Boot 3.5, Spring Security, Oracle Cloud DB, React, Docker, Vercel, Render, Cloudinary

---

## ✨ 주요 기능 및 기술적 성과 (Key Features)

* **상품 검색 최적화 (Debounce 적용)**
  상품 검색 창에 사용자가 타이핑할 때마다 발생하는 과도한 API 호출을 방지하기 위해, `setTimeout`을 활용한 **Debounce(디바운스) 로직을 프론트엔드 단에 직접 구현**했습니다. 입력을 멈춘 후 0.5초 뒤에만 서버로 요청을 보내도록 하여 불필요한 네트워크 트래픽을 줄이고 서버 부하를 최소화했습니다.

* **Oracle Cloud Wallet 기반 보안 연결 구축**
  단순한 ID/PW 방식의 데이터베이스 연결을 넘어, 실제 상용 서비스 수준의 보안을 고려하여 **Oracle Cloud Wallet을 활용한 상호 TLS(mTLS) 인증 방식**을 도입했습니다. 클라우드 DB와의 통신 구간을 암호화하여 데이터 보안성을 크게 높였습니다.

* **운영 편의성을 고려한 관리자(Admin) 통합 도구 설계**
  단순한 상품 등록을 넘어 실제 서비스 운영을 고려하여 이벤트, 공지사항 등을 관리할 수 있는 전용 API 컨트롤러(`AdminEventApiController`, `AdminNoticeApiController` 등)를 설계하고 구축했습니다. 관리자 페이지 내에서 기간 설정, 이미지 관리, 상태 동기화 등이 실시간으로 이루어지도록 비즈니스 로직을 최적화했습니다.

---

## 💡 세부 고도화 및 최적화 내역 (Details)

### 🛡️ [Ver 2.0] 관리자 운영 도구 시스템 고도화 (Notice & Event CRUD)
단순한 상품 관리를 넘어 실제 서비스 운영 환경을 고려한 **공지사항 및 이벤트 통합 관리 시스템**을 직접 설계하고 API를 구축했습니다.

* **공지사항(Notice) CRUD 시스템 구현**:
    * `AdminNoticeApiController`를 통한 게시글 등록, 수정, 삭제 전용 API 구축.
    * 운영 효율화를 위한 공지사항 목록 페이징 및 상세 조회 로직 최적화.
* **이벤트(Event) 기간 자동화 및 상태 관리**:
    * `AdminEventApiController` 및 `EventService`를 통해 이벤트 등록 시 설정한 기간과 서버 시간을 동기화.
    * `react-datepicker` 연동으로 **진행 예정 / 진행 중 / 종료** 뱃지가 UI에 실시간 반영되는 비즈니스 로직 구현.
* **운영 보안 및 어뷰징 방어**:
    * **쿠키 기반 조회수 방어**: 24시간 유효 쿠키를 활용하여 무분별한 조회수 조작을 원천 차단.
    * **이미지 최적화**: 관리자 에디터에서 대용량 이미지를 비동기 처리하여 DB 부하 및 로딩 속도 개선.

### ☁️ [Ver 2.0] 클라우드 인프라 이전 및 Docker 배포 환경 구축
* **Docker 기반 배포**: Java 21 환경 제약을 극복하기 위해 `Dockerfile`을 직접 작성하여 Render 컨테이너 배포 성공.
* **Cloudinary CDN 연동**: 서버 재시작 시 로컬 스토리지가 초기화되는 문제를 외부 스토리지 연동으로 해결.
* **Render Cold Start 대응**: 무료 티어 특성상 첫 접속 시 발생하는 로딩 지연(3~5분)을 README에 명시하여 사용자 경험(UX) 가이드 제공.

> **⚠️ Render 배포 환경 기술 참고사항**
> * **Cold Start**: Render 무료 티어 특성상 일정 시간 요청이 없으면 서버가 Sleep 상태로 전환됩니다. 첫 접속 시 컨테이너가 다시 구동되느라 약 3~5분의 로딩 지연이 발생할 수 있습니다.
> * **Java 21 컨테이너화**: 무료 티어의 제약을 극복하기 위해 `Dockerfile`을 직접 작성하여 배포 환경을 구축했습니다.
> * **이미지 영속성**: 서버 재시작 시 데이터가 초기화되는 문제를 해결하고자 **Cloudinary(CDN)** 를 연동했습니다.

### ✨ [Ver 2.0] 사용자 경험(UX) 및 안정성 개선
* **스켈레톤 UI 적용**: 서버 통신 대기 중 발생하는 텍스트 노출의 아쉬움을 해소하고자, CSS `@keyframes`를 활용한 스켈레톤 UI를 구현해 시각적 대기 시간을 단축했습니다.
* **환경변수(.env) 분리**: 퍼블릭 GitHub 리포지토리에 코드를 안전하게 공개하기 위해 프론트엔드 API 주소 및 DB 비밀번호 등 민감 정보를 환경변수로 완벽히 분리했습니다.
* **단위 테스트(JUnit/Mockito) 적용**: 코드 규모가 커짐에 따라 발생할 수 있는 사이드 이펙트를 방지하기 위해, 주요 비즈니스 로직에 단위 테스트를 작성하여 시스템 안정성을 확보하였습니다.
  
### 🚀 [Ver 1.0] 성능 최적화: 메인 페이지 로딩 속도 90% 개선
서비스 특성상 고용량 상품 이미지가 다수 노출되어 초기 배포 후 메인 페이지 로딩에 **7초 이상 소요되는 병목 현상**이 발생했습니다.
* **해결 과정**: 압축률이 높은 WebP 도입을 검토했으나 환경 제약으로 난항을 겪었습니다. 사용자 불편을 즉각 해소하기 위해 **Thumbnailator 라이브러리를 활용한 최적화**를 적용했습니다. 원본 6MB 이상의 이미지를 화질 저하 없이 **1MB 이하(약 85% 감소)** 로 압축했습니다.
* **결과**: 메인 페이지 로딩 속도를 **1초 이내로 단축(90% 향상)** 하여 쾌적한 쇼핑 환경을 구축했습니다.

### 🤝 [Ver 1.0] 프론트엔드 예외 처리 및 팀 프로세스 개선
* **전역 인증/에러 핸들링**: 토큰 만료(401) 시 API 단에서 요청을 가로채어 안전하게 로컬 데이터를 삭제하고 로그인 페이지로 유도하는 등 직관적인 커스텀 에러 UI 처리를 진행했습니다.
* **DTO 네이밍 컨벤션 정립**: 프론트엔드와 백엔드 간 변수명 불일치(`CartQty` vs `OrderQty`)로 발생한 연동 오류를 디버깅을 통해 규명하고, **API 명세서 최신화 및 네이밍 컨벤션 통일**을 제안해 팀 협업 효율을 높였습니다.

---

## 🛠️ 기억에 남는 트러블 슈팅 (Troubleshooting)

### 폼 데이터(FormData) 전송 시 Content-Type 충돌 해결
* **문제**: 에디터 이미지를 백엔드로 비동기 전송할 때 `MultipartException`이 발생하며 업로드가 거부됨.
* **원인 및 해결**: 프론트엔드 fetch 요청 시 `headers`에 `Content-Type: multipart/form-data`를 수동으로 명시한 것이 원인이었습니다. 브라우저가 파일의 boundary 값을 포함해 자동으로 헤더 생성하도록 수동 설정을 제거하여 정상적인 파일 업로드를 구현했습니다.

### DTO 매핑 누락으로 인한 1970년 날짜 렌더링 버그 해결
* **문제**: 이벤트 종료일을 정상 선택했음에도 화면에 "1970년 1월 1일"로 노출되는 현상 발생.
* **원인 및 해결**: 백엔드에서 `endDate: null`을 반환하는 것을 네트워크 응답 탭에서 확인했고, `EventResponseDTO` 변환 과정의 필드 매핑 누락이 원인이었습니다. **"에러는 원인이 발생한 계층에서 근본적으로 해결해야 한다"** 는 원칙에 따라, 프론트엔드의 예외 처리 대신 백엔드 DTO 코드를 직접 수정하여 바로잡았습니다.

### Cloudinary API 연동 중 발생한 카멜 케이스(CamelCase) 에러
* **문제**: Cloudinary 연동 후 서버 기동 시 `Must supply api_key` 에러와 함께 서버가 다운됨.
* **원인 및 해결**: 자바 관습에 따라 `config.put("apiKey", apiKey)`로 작성했으나, 해당 라이브러리는 스네이크 케이스(`api_key`)만 인식했습니다. 이를 통해 외부 라이브러리 연동 시 익숙한 관습보다 **공식 문서와 컨벤션을 최우선으로 준수**해야 함을 체득했습니다.

---

## 🌱 프로젝트 회고 및 인사이트
팀 프로젝트로 진행했던 초기(Ver 1.0)에는 제한된 시간 내에 주어진 요구사항을 구현하고 **기능이 정상적으로 동작하는 코드**를 완성하는 데 집중했습니다. 

하지만 개인 고도화(Ver 2.0)를 통해 인프라를 이전하고 배포까지 진행하는 과정에서는 사용자 관점의 로딩(스켈레톤 UI), 악의적인 트래픽에 대한 서버의 방어(조회수 어뷰징 방지) 등 **실제 서비스 운영 환경을 깊이 있게 고민**해 볼 수 있었습니다.

또한, 문제를 마주했을 때 단편적인 현상만 수정하기보다 쿼리 로그와 네트워크 탭을 분석하여 **근본적인 원인을 추적하는 습관**이 생겼습니다. 사소한 오타나 설정 누락이 전체 시스템에 미치는 영향을 직접 체감하며, 꼼꼼한 코드 리뷰와 테스트 코드 작성의 중요성을 체득한 값진 경험이었습니다.